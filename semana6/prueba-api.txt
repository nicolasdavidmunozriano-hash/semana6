Al enviar una petición POST a la ruta http://localhost:3000/registro con un objeto JSON, el servidor recibe los datos mediante req.body y responde con otro objeto JSON que confirma la recepción de la información.

Respuesta esperada:

{
  "estado": "Datos recibidos",
  "nombre": "Maria",
  "mensaje": "Hola comunidad"
}
